/** @type {import('next').NextConfig} */
const nextConfig = {
    // async rewrites() {
    //     return [
    //         {
    //             source: '/posts/:id*',
    //             destination: '/:id*', // The :path parameter is used here so will not be automatically passed in the query
    //         },
    //     ]
    // },
}

module.exports = nextConfig